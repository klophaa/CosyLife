{
  "name": "cosylife",
  "version": "0.1.0",
  "private": true,
  "description": "Application mobile PWA pour simplifier la vie: taches, budget, listes et organisation.",
  "scripts": {
    "dev": "node local-server.js",
    "check": "node --check app.js && node --check service-worker.js && node --check local-server.js"
  }
}